# Returns correct output format for mixed columns for 'random' initialization

    Code
      cfactuals = mocr$find_counterfactuals(x_interest, desired_outcome)
    Message <simpleMessage>
      `x_interest` was removed from results.

