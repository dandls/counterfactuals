# $check_desired_outcome returns error message if desired_outcome has incorrect formats

    `desired_outcome` is invalid.
    x The lower bound of `desired_outcome` cannot be higher than the upper bound.

